import 'package:flutter/material.dart';

class Tema{
  Color primary;
  Color primaryText;
  Color body;
  Color text;
  String ime;

  Tema({@required this.primary, @required this.primaryText, @required this.body, @required this.text, @required this.ime});
}